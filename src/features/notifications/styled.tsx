import { styled } from 'styled-components';

export const Wrapper = styled.div`
  position: fixed;
  bottom: 0;
  right: 0;
  padding: 1.2rem;
`

export const Grid = styled.div`
  display: grid;
  gap: 1rem;
`